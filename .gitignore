# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/

# Data & models — too large / regenerable
data/raw/*.csv
data/processed/*.csv
data/processed/*.parquet
models/*.joblib
models/*.pkl
# ...except the final production bundle, which the Docker image needs.
# 611 KB — committed for deployment convenience (regenerable via
# `python -m src.finalize_model` if the data and intermediate artifacts exist).
!models/fraud_model.joblib

# Frontend (added in Phase 4)
node_modules/
.next/
.vercel/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# Secrets
.env
.env.local
*.key
